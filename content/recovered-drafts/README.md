# recovered drafts

if a draft was written to the wrong place, or if hidden content state was edited directly, the system moves that work here instead of deleting it.

if something vanished, look here first.

to resubmit a recovered draft:

```bash
npm run content -- recovery-list
npm run content -- recovery-restore <id>
npm run content -- submit content/submit-here/<restored-file>.md
```
